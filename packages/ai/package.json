{
  "name": "@news-lens-veritas/ai",
  "version": "0.1.0",
  "description": "AI agent infrastructure — base class, runtime, tracking",
  "main": "./src/index.ts",
  "private": true,
  "exports": {
    ".": "./src/index.ts",
    "./category-classifier": "./agents/src/category-classifier/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "type-check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "ai": "^4.3.0",
    "@anthropic-ai/sdk": "^0.39.0",
    "zod": "^3.23.0",
    "@news-lens-veritas/db": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.3.0",
    "vitest": "^2.0.0"
  }
}
